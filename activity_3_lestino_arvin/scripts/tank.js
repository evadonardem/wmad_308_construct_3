export function create(runtime, count)
{
for(let i=0; i<1; i++){

	const tank=runtime.objects.
	tank2.createInstance(150,150, 150);
	
	tank.width = tank.width * 0.30;
	tank.height = tank.height * 0.30;
	tank.angle = 90;
	
	tank.x=i * tank.width;
}
}